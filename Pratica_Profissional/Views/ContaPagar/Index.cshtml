@using Pratica_Profissional.Util.Class;
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var _action = ViewContext.Controller.ValueProvider.GetValue("action").RawValue.ToString();
    var _controller = ViewContext.Controller.ValueProvider.GetValue("controller").RawValue.ToString();
}

@if (@TempData["message"] != null)
{
    if (@TempData["type"].ToString() == "sucesso")
    {
        <div class="alert alert-success" id="success-alert">

            <a class="close" data-dismiss="alert">&times;</a>

            <strong style="width:12px">Sucesso!</strong> @TempData["message"]

        </div>
    }

    if (@TempData["type"].ToString() == "erro")
    {
        <div class="alert alert-danger" id="success-alert">

            <a class="close" data-dismiss="alert">&times;</a>

            <strong style="width:12px">Erro!</strong> @TempData["message"]

        </div>
    }
}
<h1 class="d-flex justify-content-center">Consultar ordens de serviço</h1>

<p class="d-flex justify-content-end">
    <button type="button" class="btn btn-success" onclick="location.href='@Url.Action("Create", "ContaPagar")'"><i class="fa fa-plus"></i>Adicionar</button>
</p>

<table id="lista" class="table table-striped table-bordered" style="width:100%" data-url="@Url.Action("JsSearch")">
    <thead class="thead-dark">
        <tr>
            <th export="true" style="width:5%;">Parcela</th>
            <th export="true" style="width:5%;">Modelo</th>
            <th export="true" style="width:5%">Série</th>
            <th export="true" style="width:5%">Número</th>
            <th export="true" style="width:25%">Fornecedor</th>
            <th export="true" style="width:9%;">Valor</th>
            <th export="true" style="width:9%;">Vencimento</th>
            <th export="true" style="width:12%;">Situação</th>
            <th></th>
        </tr>
    </thead>
</table>

<script src="~/Scripts/ContaPagar/Index.js"></script>
<script src="~/Scripts/Util.js"></script>
<script>
    $(document).ready(function () {
        $('#tabela').DataTable({
            language: {
                url: 'https://cdn.datatables.net/plug-ins/1.10.24/i18n/Portuguese-Brasil.json'
            }
        });
    });

    $("#success-alert").fadeTo(5000, 500).slideUp(500, function () {
        $("#success-alert").slideUp(500);
    });
</script>
